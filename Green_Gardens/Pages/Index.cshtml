@page
@using Green_Gardens.Model
@using static Green_Gardens.Model.Customer
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome To Green Gardens</h1>

    @if (!User.Identity.IsAuthenticated)
    {
        <form method="get" asp-page="/Login">
            <button type="submit" class="btn btn-primary">Login</button>
        </form>
    }

    @if (User.Identity.IsAuthenticated)
    {
        <form method="get" asp-page="/Logout">
            <button type="submit" class="btn btn-warning">Logout</button>
        </form>
    }
</div>

<table class="table;" style="text-align:center; width:100%;">
    <thead>
        <tr>
            <th style="padding-top:20px;">Check out our shop</th>
        </tr>
    </thead>
    <tbody>
            <tr>
                <td>
                <img src="~/images/shop/shop.png" alt="Shop" style="width:400px;">
                </td>
            </tr>
                
    </tbody>
    <tfoot>
        <tr>
            <td>
                <form method="get" asp-page="/Products">
                    <button type="submit" class="btn btn-primary">Product Page</button>
                </form>
            </td>
        </tr>
    </tfoot>
</table>